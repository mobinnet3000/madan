# 📒 گزارش آخرین تغییرات (CHANGELOG)

> تاریخ: مرداد ۱۴۰۵ — نسخه توسعه (Local)
> تمام تغییرات هم در **بک‌اند (Django)** و هم در **فرانت‌اند (React)** اعمال و تست شده‌اند.

---

## ۱. کد دستگاه (جدا از نام)

- **بک‌اند:** فیلد `code` به مدل `Device` اضافه شد (migration `0006`)؛ نام دستگاه با «نام» و «کد» از هم جدا شد.
  - سریالایزر `DeviceSerializer` مقدار `code` را برمی‌گرداند.
  - پنل ادمین: فیلد `code` در فرم و لیست دستگاه، و در اینلاین دستگاه‌های خط نمایش داده می‌شود.
- **فرانت‌اند:** نمایش کد در کنار نام دستگاه در نمای خط (LineFlow)، لیست گزارش عملکرد، و فیلد کد در اطلاعات دستگاه.
- **Seed:** برای هر دستگاه یک کد خودکار (مثل `L1-D01`) تولید می‌شود.

## ۲. واحد اندازه‌گیری ویژگی‌های فنی (خط و دستگاه) — پذیرش «توان (w)»
- فیلد `unit` در `Attribute` و `ProductionLineAttribute` از قبل وجود داشت و **هر کاراکتری ازجمله `w`/`Kw` را می‌پذیرد** (بدون محدودیت).
- **فرانت‌اند:** واحد هر ویژگی در کنار مقدار نمایش داده می‌شود (مثلاً `کیلووات` یا `تن/ساعت`).
- ویژگی‌های هر خط و هر دستگاه به‌همراه **واحدشان** از طریق API در اختیار فرانت قرار می‌گیرد (`attribute_defs`).

## ۳. تاریخ شمسی + روز هفته
- **بک‌اند:**
  - کتابخانه `jdatetime` به `requirements.txt` اضافه شد.
  - ماژول جدید `machines/jalali.py` برای تبدیل میلادی→شمسی و روز هفته.
  - فیلدهای `date_jalali` و `day_of_week` به خروجی گزارش عملکرد و آنالیز اضافه شد.
  - فیلد `timestamp_jalali` برای لاگ فعالیت‌ها.
  - گزارش‌های PDF/Excel: برچسب بازه و ستون تاریخ به شمسی + روز هفته.
- **فرانت‌اند:** توابع تبدیل شمسی در `utils.ts`؛ تمام جدول‌ها و پیش‌نمایش تاریخ‌ها شمسی شده و روز هفته نمایش داده می‌شود (مثلاً «پنجشنبه · ۱۴۰۵/۰۵/۱۵»).

## ۴. دراپ‌داون‌های آبشاری (وابسته)
- در فرم گزارش عملکرد: انتخاب **خط** باعث می‌شود:
  - **شیفت‌ها** فقط برای همان خط/کارخانه فیلتر شوند،
  - **دستگاه‌ها** فقط برای همان خط فیلتر شوند،
  - و انتخاب شیفت/دستگاه با تغییر خط پاک شود.
- همین الگو در فرم گزارش تولید نیز رعایت شده است.

## ۵. ساعت توقف + کارکرد مفید (به‌جای کارکرد/توقف جدا)
- در فرم «ثبت گزارش عملکرد» به‌جای دو فیلد جدا، فقط **«ساعت توقف»** وارد می‌شود.
- **«ساعت کارکرد مفید» به‌صورت خودکار** = طول شیفت − ساعت توقف محاسبه و نمایش داده می‌شود (شیفت شب درست محاسبه می‌شود).
- هنگام ذخیره، همین مقدار محاسبه‌شده در `runtime_hours` ثبت می‌شود.

## ۶. جداسازی «گزارش‌های تولید» از «گزارش عملکرد روزانه»
- بخش «اطلاعات تولیدی خط» (تناژ ورودی/محصول/باطله) **از فرم گزارش عملکرد روزانه و از جدول آن حذف شد**.
- مدل و API جدید **`ProductionReport`** ساخته شد:
  - اندپوینت `GET/POST/PATCH/DELETE /api/production-reports/`
  - دسترسی مدیر/اپراتور فقط به داده‌های کارخانه‌ی خودش محدود می‌شود.
- **منوی جدید «گزارش‌های تولید»** در سایدبار + صفحه‌ی جدید با **فیلتر بازه تاریخ (از/تا)** به‌جای یک روز.
- صفح برای گزارش تولید: خط → بازه تاریخ → تناژ ورودی/محصول/باطله → بازدهی خودکار.

## ۷. از کجا مقادیر ویژگی‌ها را پر کنیم؟ (پاسخ به سؤال شما)
- قبل از این تغییر، فقط از **پنل ادمین Django** امکان‌پذیر بود.
- حالا **در خود برنامه (فرانت‌اند)**:
  - روی هر خط در صفحه «مدل‌سازی خط فرآوری» دکمه **«ویرایش ویژگی‌ها»** اضافه شد؛ با کلیک روی آن، پنجره‌ای با لیست ویژگی‌ها (نام + واحد) و فیلد مقدار باز می‌شود.
  - روی هر **دستگاه** در همان صفحه، آیکون ویرایش (چرخ‌دنده) هنگام هاور ظاهر می‌شود و به‌همان شیوه می‌توان مقادیر ویژگی‌های دستگاه را پر کرد.
- اندپوینت‌های جدید: `GET/PATCH /api/lines/{id}/attributes/` و `GET/PATCH /api/devices/{id}/attributes/`.

---

## فایل‌های تغییر یافته/جدید
### بک‌اند
- `machines/models.py` (فیلد code + مدل ProductionReport)
- `machines/migrations/0006_*.py` (مایگریشن جدید)
- `machines/jalali.py` (جدید)
- `machines/serializers.py` (کد، attribute_defs، تاریخ شمسی، ProductionReport)
- `machines/views.py` (ProductionReportViewSet + اندپوینت ویژگی‌ها)
- `machines/urls.py` (مسیرهای جدید)
- `machines/filters.py` (ProductionReportFilter)
- `machines/reports.py` (تاریخ شمسی)
- `machines/admin.py` (ثبت ProductionReport + کد دستگاه)
- `accounts/serializers.py` (timestamp_jalali)
- `seed_demo.py` (کد خودکار دستگاه)
- `requirements.txt` (+jdatetime)

### فرانت‌اند
- `src/utils.ts` (توابع شمسی + روز هفته + مدت شیفت)
- `src/types.ts`
- `src/api/production.ts` (جدید)
- `src/pages/Logs.tsx` (بازنویسی فرم و جدول)
- `src/pages/ProductionReports.tsx` (جدید)
- `src/components/AttributeEditor.tsx` (جدید)
- `src/components/LineFlow.tsx` (کد دستگاه + ویرایش ویژگی‌ها)
- `src/pages/Lines.tsx` (ویرایش ویژگی خط/دستگاه)
- `src/pages/Analysis.tsx` (تاریخ شمسی)
- `src/pages/ActivityLog.tsx` (تاریخ شمسی)
- `src/App.tsx` و `src/components/layout/Sidebar.tsx` (مسیر/منوی جدید)

## نکته‌ی اجرا
دیتابیس لوکال دوباره seed شد (شامل کدهای دستگاه‌ها). کافی است:
```
start.bat
```
یا دستی: بک‌اند روی ۸۰۰۰ و فرانت‌اند روی ۵۱۷۳.