"""
Production overrides for cPanel.
Import at bottom of core/settings.py with:
    from .settings_production import *
"""
# All these are already set in settings.py — just keeping for reference.
# Adjust via .env if needed.
pass
